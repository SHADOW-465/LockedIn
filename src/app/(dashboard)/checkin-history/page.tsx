'use client'

import { RebuildStub } from '@/components/rebuild-stub'

export default function CheckinHistoryPage() {
  return (
    <RebuildStub
      title="Check-in history"
      description="Mood check-ins — POST /api/mood/checkin still available."
    />
  )
}
