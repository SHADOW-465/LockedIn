'use client'

import { RebuildStub } from '@/components/rebuild-stub'

export default function JournalPage() {
  return <RebuildStub title="Journal" description="Journal UI stripped — user-create API remains." />
}
